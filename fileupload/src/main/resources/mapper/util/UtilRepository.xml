<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.einz.fileupload.util.repository.FileUploadRepository">
    <select id="selectAllTodos" resultType="com.einz.fileupload.util.model.Todo">
        SELECT
           todoid,
           content,
           completed
        FROM TODO_9
    </select>   
</mapper>